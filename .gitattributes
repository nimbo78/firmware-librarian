# Хранить и выгружать текст с LF на всех платформах.
# Критично для redeploy.sh: CRLF в shebang ломает выполнение на Linux-NAS.
* text=auto eol=lf

# Бинарные файлы не трогать (на случай, если попадут в контекст сборки).
*.session binary
*.sqlite binary
